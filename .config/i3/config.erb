# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: <%= i3['font'] %>
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec bash ~/bin/i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill
bindsym ctrl+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec "rofi -show || dmenu_run"
bindsym $alt+Tab exec "rofi -show"

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# toggle sticky
bindsym $mod+Shift+s sticky toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Execute xkill
bindsym $mod+Escape exec "xterm -name floating -geometry 40x4 -e xkill"
bindsym $mod+F1 exec "xterm -name floating -geometry 80x40 +ls -e '~/bin/get_commands'"

set $workspace1 "1: "
set $workspace2 "2: "
set $workspace3 "3: "
set $workspace4 "4: "
set $workspace5 "5: "
set $workspace6 "6: "
set $workspace7 "7: "
set $workspace8 "8: "
set $workspace9 "9: "
set $workspace10 "10: "


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10

bindsym $mod+KP_1 workspace $workspace1
bindsym $mod+KP_2 workspace $workspace2
bindsym $mod+KP_3 workspace $workspace3
bindsym $mod+KP_4 workspace $workspace4
bindsym $mod+KP_5 workspace $workspace5
bindsym $mod+KP_6 workspace $workspace6
bindsym $mod+KP_7 workspace $workspace7
bindsym $mod+KP_8 workspace $workspace8
bindsym $mod+KP_9 workspace $workspace9
bindsym $mod+KP_0 workspace $workspace10

bindsym KP_End workspace $workspace1
bindsym KP_Down workspace $workspace2
bindsym KP_Next workspace $workspace3
bindsym KP_Left workspace $workspace4
bindsym KP_Begin workspace $workspace5
bindsym KP_Right workspace $workspace6
bindsym KP_Home workspace $workspace7
bindsym KP_Up workspace $workspace8
bindsym KP_Prior workspace $workspace9
bindsym KP_Insert workspace $workspace10

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# ruby installer
bindsym $mod+Shift+i exec "tmp=$(mktemp);tmp1=$(mktemp);ruby ~/dotfiles/install.rb 2>${tmp} >/${tmp1} && notify-send \\"Ruby install success\\" \\"$(cat ${tmp1})\\"|| notify-send \\"Ruby installer error\\" \\"$(cat ${tmp})\\"; rm -f ${tmp}; rm -f ${tmp1}"

# restart polybar
bindsym $mod+Shift+p exec bash ~/bin/restart-polybar

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px
        bindsym k resize grow height 10 px
        bindsym l resize shrink height 10 px
        bindsym semicolon resize grow width 10 px

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px
        bindsym Down resize grow height 10 px
        bindsym Up resize shrink height 10 px
        bindsym Right resize grow width 10 px

        bindsym Shift+Left resize shrink width 50 px
        bindsym Shift+Down resize grow height 50 px
        bindsym Shift+Up resize shrink height 50 px
        bindsym Shift+Right resize grow width 50 px


        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"


# window colors
#                       border       background   text         indicator
client.focused          <%= i3['client']['focused'] %>
client.unfocused        <%= i3['client']['unfocused'] %>
client.focused_inactive <%= i3['client']['focused_inactive'] %>
client.urgent           <%= i3['client']['urgent'] %>

# Default workspaces
assign [class=".*Slack$"]                                                               $workspace6
assign [class="Rambox"]                                                                 $workspace6
assign [class="Thunderbird"]                                                            $workspace6
#assign [class="Google-chrome"]                                                         $workspace1
#assign [class="Chromium"]                                                              $workspace1
assign [class="Gnome-terminal"]                                                         $workspace2
assign [class="jetbrains-phpstorm"]                                                     $workspace4
assign [class="Vmplayer"]                                                               $workspace8
assign [class=".*Wunderlist$"]                                                          $workspace7
assign [class=".*1password"]                                                            $workspace7
assign [class="Thunar"]                                                                 $workspace3
assign [class="KeePass2"]                                                               $workspace5
assign [class="KeeWeb"]                                                                 $workspace5
assign [class="TeamViewer"]                                                             $workspace8


# For windows
for_window [class=".*"]                                                                 title_format " %title"
for_window [class="Yad"]                                                                floating enable
for_window [class="mpv"]                                                                floating enable
for_window [instance=".*floating.*"]                                                    floating enable
for_window [title=".*Wunderlist$"]                                                      move container to workspace $workspace7
for_window [title=".*Slack$"]                                                           move container to workspace $workspace6
for_window [class="Rambox"]                                                             move container to workspace $workspace6
for_window [window_role="pop-up"]                                                       floating enable
for_window [window_role="bubble"]                                                       floating enable
for_window [window_role="task_dialog"]                                                  floating enable
for_window [window_role="Preferences"]                                                  floating enable
for_window [window_type="dialog"]                                                       floating enable
for_window [window_type="menu"]                                                         floating enable
for_window [class="TeamViewer"]                                                         floating enable
# Float IntelliJ update window
for_window [class="sun-awt-X11-XDialogPeer"]                                            floating enable
for_window [class="com-intellij-updater-Runner"]                                        floating enable
for_window [class="Gloobus-preview"]                                                    floating enable, border none
# source: https://github.com/Airblader/dotfiles-manjaro/blob/master/.i3/config
#for_window [class="(?i)VirtualBox" title="(?i)Manager"]                                floating enable
#for_window [class="(?i)VirtualBox"]                                                    floating enable
for_window [class="(?i).*chrome.*"]                                                     border pixel <%= i3['border'] %>
for_window [class="(?i).*chromium.*"]                                                   border pixel <%= i3['border'] %>
for_window [class="(?i).*xterm.*"]                                                      border pixel <%= i3['border'] %>
for_window [class="(?i).*termite.*"]                                                    border pixel <%= i3['border'] %>
for_window [class="(?i).*jetbrains.*"]                                                  border pixel <%= i3['border'] %>



# Execs
exec --no-startup-id redshift
exec --no-startup-id udiskie
exec --no-startup-id rambox
exec --no-startup-id flameshot

# Exec always
exec_always --no-startup-id killall imwheel; while pgrep -x imwheel >/dev/null; do sleep 1; done; imwheel
exec_always --no-startup-id killall parcellite; while pgrep -x parcellite >/dev/null; do sleep 1; done; parcellite
exec_always --no-startup-id killall nm-applet; while pgrep -x nm-applet >/dev/null; do sleep 1; done; nm-applet
exec_always --no-startup-id bash ~/bin/restart-polybar || xfce4-panel
exec_always --no-startup-id killall dunst; while pgrep -x dunst >/dev/null; do sleep 1; done; dunst
exec_always --no-startup-id bash -c "source ~/.functions; setRandomWallpaper"
#exec_always --no-startup-id killall compton; while pgrep -x compton >/dev/null; do sleep 1; done; compton --config ~/.config/compton.conf -b --dbe
exec_always --no-startup-id killall xbanish; while pgrep -x xbanish >/dev/null; do sleep 1; done; xbanish
exec_always --no-startup-id killall volumeicon; while pgrep -x volumeicon >/dev/null; do sleep 1; done; volumeicon
exec_always --no-startup-id killall synergys; while pgrep -x synergys >/dev/null; do sleep 1; done; synergys -c ~/.synergy.conf

#exec_always --no-startup-id pkill -f resolution-changed; bash ~/bin/resolution-changed ~/bin/restart-polybar
exec_always --no-startup-id bash "$HOME/.screenlayout/$(hostname).sh"

# Custom bindsym
bindsym $mod+Shift+greater move workspace to output right
bindsym $mod+Shift+less move workspace to output left
bindsym Scroll_Lock exec bash ~/.config/i3/scripts/lock
bindsym $mod+Shift+o exec ruby ~/bin/copy-tmux-sidebar-command.rb
bindsym Print exec flameshot gui
bindsym $mod+Print exec flameshot full -p ~/Pictures/ -c

# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume $(pactl list short sinks | grep alsa_output | cut -f1) +2% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume $(pactl list short sinks | grep alsa_output | cut -f1) -2% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute $(pactl list short sinks | grep alsa_output | cut -f1) toggle # mute sound


# Media player controls
bindsym XF86AudioPlay exec mpc toggle
bindsym XF86AudioPause exec mpc toggle
bindsym XF86AudioNext exec mpc next
bindsym XF86AudioPrev exec mpc prev


bindsym $mod+ctrl+u exec mpc prev
bindsym $mod+ctrl+i exec mpc stop
bindsym $mod+ctrl+o exec mpc toggle
bindsym $mod+ctrl+p exec mpc next

# Screen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Gaps
gaps inner <%= i3['gaps']['inner'] %>
gaps outer <%= i3['gaps']['outer'] %>

# for_window [class="^.*"] border pixel <%= i3['border'] %>

# new_float none


# FOCUS
focus_on_window_activation urgent



# Display

## Manual management of external displays
# Set the shortcuts and what they do
set $mode_display Display mode [w]ork dual displays, [s]ingle display Full HD, s[i]ngle display 1440x900
mode "$mode_display" {
    bindsym w exec --no-startup-id bash ~/.screenlayout/work.sh
    bindsym s exec --no-startup-id bash ~/.screenlayout/1-display-full-hd.sh
    bindsym i exec --no-startup-id bash ~/.screenlayout/1-display-1400x900.sh


 # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
# Declare here the shortcut to bring the display selection menu
bindsym $mod+x mode "$mode_display"


# Power management
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"

 # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Pause mode "$mode_system"
